import Standard.Test.Box (
    Box_Test_Suite
);

module body Standard.Test is
    function Main(root: Root_Capability): Root_Capability is
        Box_Test_Suite();
        return root;
    end;
end module body.
